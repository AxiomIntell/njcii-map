name: Merge firms data
on:
  push:
    paths:
      - '.github/workflows/merge_firms.yml'

permissions:
  contents: write

jobs:
  merge:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Merge parts
        run: |
          python3 -c "
import json
with open('data/firms_00_part1.json') as f:
    part1 = json.load(f)
with open('data/firms_00_part2.json') as f:
    part2 = json.load(f)
combined = part1 + part2
with open('data/firms_00.json', 'w') as f:
    json.dump(combined, f)
print(f'Combined {len(combined)} records into firms_00.json ({len(json.dumps(combined))} bytes)')
"
      - name: Cleanup and commit
        run: |
          git config user.name 'github-actions[bot]'
          git config user.email 'github-actions[bot]@users.noreply.github.com'
          git add data/firms_00.json
          git rm -f data/firms_00_part1.json data/firms_00_part2.json || true
          git rm -f .github/workflows/merge_firms.yml || true
          git diff --cached --quiet || git commit -m 'Fix firms_00.json - restore all 20 records'
          git push
